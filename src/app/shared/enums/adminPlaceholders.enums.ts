export enum TitlePlaceholder {
  default = 'Please enter a title',
  valid = 'The title is valid',
  short = 'The title is too short',
  long = 'The title is too long',
}

export enum DescriptionPlaceholder {
  default = 'Please enter a description',
  valid = 'The description is valid',
  invalid = 'The description is too long',
}

export enum ImgPlaceholder {
  default = 'Please enter a link to the image',
  valid = 'The image link is valid',
  invalid = 'The image link is invalid',
}

export enum LinkVideoPlaceholder {
  default = 'Please enter a link to the video',
  valid = 'The video link is valid',
  invalid = 'The video link is invalid',
}

export enum CreationDatePlaceholder {
  default = 'Please enter a creation date',
  valid = 'The date is valid',
  invalid = 'The date is invalid',
}
