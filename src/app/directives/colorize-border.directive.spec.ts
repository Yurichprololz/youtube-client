import { ColorizeBorderDirective } from './colorize-border.directive';

describe('ColorizeBorderDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorizeBorderDirective();
    expect(directive).toBeTruthy();
  });
});
